{
  "buildCommand": "cd web-legacy && npm install && npm run build",
  "outputDirectory": "web-legacy/.next",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD .",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/web-legacy/$1"
    }
  ],
  "framework": "nextjs"
}
